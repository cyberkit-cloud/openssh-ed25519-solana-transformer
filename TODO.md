[x] - Implement Open SSH public string to Solana Address
[x] - Implement OPENSSH private key format to Solana Private key and thus address and secret.
[ ] - We should remove the need for Buffer.from so that we can use Uint8Array to be browser compatible
[x] - Implement Solana Address to Open SSH public string
[x] - Implement Solana Private Key to OPENSSH format
